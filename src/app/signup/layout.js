export const metadata = {
  title: "Sign Up - DocAppoint to manage your doctor appointments",
  description: "Create a new account on DocAppoint to manage your doctor appointments",
};

export default function SignUpLayout({ children }) {
  return children;
}
