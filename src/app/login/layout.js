export const metadata = {
  title: "Login - DocAppoint to manage your doctor appointments",
  description: "Login to your DocAppoint account to manage your doctor appointments",
};

export default function LoginLayout({ children }) {
  return children;
}
